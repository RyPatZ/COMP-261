public class Articulation {
    Node firstNode;
    int Depth;
    Node root;

    Articulation(Node node, int depth, Node root){
     this.firstNode = node;
     this.Depth=depth;
     this.root=root;

    }
}
